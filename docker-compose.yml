services:
  app:
    build: .
    container_name: node_express_app
    ports:
      - "3030:3030"
    volumes:
      - .:/app       
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: ["node", "index.js"]
